import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import { ThemeProvider } from "@/components/providers/theme-provider";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Fintrack - Smart Expense Tracking",
  description:
    "Track your expenses effortlessly with Fintrack. Get clear insights, beautiful charts, and smart summaries to manage your finances better.",
  keywords: ["expense tracker", "finance", "budgeting", "money management"],
  authors: [{ name: "Fintrack" }],
  openGraph: {
    title: "Fintrack - Smart Expense Tracking",
    description: "Track your expenses effortlessly with clear insights and beautiful analytics.",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" suppressHydrationWarning>
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased min-h-screen bg-background`}
      >
        <ThemeProvider
          attribute="class"
          defaultTheme="system"
          enableSystem
          disableTransitionOnChange
        >
          {children}
        </ThemeProvider>
      </body>
    </html>
  );
}
